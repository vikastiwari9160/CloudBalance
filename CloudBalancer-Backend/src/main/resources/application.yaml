spring:
  application:
    name: CloudBalancer
  datasource:
    url: jdbc:mysql://localhost:3306/CloudBalance
    username: root
    password: NewPassword123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: NewSecretKeyForJWT-is-here-with-54321
  expiration: 36000
